# Journeys (Nuxt) — TODO

- Documenter `useJourneyEngine` (schema P1, navigation E0→E6) et conventions de mapping slug → journeyId.
- Décrire les clés de stockage locales par parcours : P1 utilise `pp_journey_p1_scores_v1` / `pp_journey_p1_meta_v1` (TTL ~30 jours), aucune réponse brute persistée.
- Aligner les conventions d’analytics coarse (q1_start/q1_complete) déclenchées via le moteur.
